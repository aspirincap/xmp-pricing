---
name: xmp-business-quote
description: XMP 商务报价、套餐推荐与折扣测算。只要用户提到 XMP 报价、基础版/高级版/专业版/VIP、媒体数量、Ad 创建额度、用户或广告账号规模、API、流量池、试新、人民币或美元价格、推荐价/几折，或粘贴销售需求要求算报价，就应使用本 Skill。将自然语言或截图中的需求解析为结构化输入，调用确定性脚本枚举套餐、抵扣内含额度、计算必要加购和减去项，并输出原活动价、调整后活动价、6/7/8/9 折报价、最终折扣及完整报价构成。不要用于非 XMP 产品报价或广告投放策略咨询。
---

# XMP 商务报价

把销售话术转换为可审计的报价。模型负责理解语言，`scripts/calculate_quote.py` 负责所有金额、套餐枚举、取整、减去项和折扣计算。不要在对话中自行心算后替代脚本结果。

## 初始化与每次执行

0. **仅在 Skill 初始化时获取报价单。** 首次安装或初始化本 Skill 时运行：

  ```bash
  python3 <skill-directory>/scripts/initialize_pricing.py --strict
  ```

  脚本会读取 USD/CNY 官方页面，并将核验结果保存为 `<skill-directory>/references/pricing-check.json`。只有初始化返回 `status: "verified"` 才能启用正式报价；若网络不可用、页面价格标记缺失或与本地快照不一致，先更新价格参考与测试。
  后续每次报价直接复用该初始化快照，不再强制访问官网。需要主动刷新时再次运行初始化脚本。
1. 阅读 `references/business-rules.md`，遵守业务口径。
2. 按 `references/request-schema.md` 从用户文本或截图提取请求。
3. 只有关键输入存在歧义且会改变金额时才追问；其他缺失字段保持“不确定/未提供”，不要自动填 0。
4. 将请求保存到临时 JSON。定位本 Skill 的目录后，用绝对路径调用计算器；计算器默认读取初始化快照，也可显式传入 `--pricing-check`：

   ```bash
   python3 <skill-directory>/scripts/calculate_quote.py \
     --input /tmp/xmp-quote-request.json \
     --format markdown
   ```

5. 直接使用计算器的 Markdown 结果。可以补一句解释，但不得改写金额或遗漏减去项、额度汇总、四档对比、报价单获取时间和来源。

若用户要机器可读结果，使用 `--format json`。

## 币种路由

- 出现“人民币、RMB、CNY、¥”时使用 `cny`。
- 出现“美元、美金、USD、$”时使用 `usd`。
- 用户未指定币种时，分别生成 CNY 和 USD 两套结果，不做汇率换算。

只阅读与币种对应的价格说明：

- CNY：`references/pricing-cny.md`
- USD：`references/pricing-usd.md`

## 自然语言解析

### 套餐

- 默认自动推荐，不设置 `locked_plan`。
- 用户明确说“必须/指定/就按专业版”等，设置 `locked_plan` 为 `basic`、`advanced`、`pro` 或 `vip`。
- 指定套餐时仍输出自动推荐套餐的对比，但不偷偷替换用户指定方案。
- 区分“总需求”和“报价组合”。“使用 1 个大媒体”表示客户总需求 1 个；“基础版 + API + 1 个大媒体”表示锁定基础版，并在套餐自带额度外加购 1 个大媒体。后一种应设置 `locked_plan: "basic"`、`report_api: true`，并把大媒体总需求写为基础版已含 1 个 + 增购 1 个，即 `major_media: 2`。若加号语义不清才追问。

### 媒体

- 大媒体：Meta/Facebook/FB、Google、TikTok/TT、Snapchat、Kwai、Apple Ads。去重后写入 `major_media`。
- 视频渠道：Mintegral、Unity、AppLovin、Liftoff。去重后写入 `video_channels`。
- 只有大媒体产生额外渠道费用。视频渠道不加价，但超过套餐容量时该套餐不可用。

### Ad 创建额度

- “每月/月度 Ad”写入 `monthly_ads_wan`，单位为万/月。
- “不过期/永久 Ad”写入 `non_expiring_ads_wan`，单位为万。
- “50w不过期Ad”表示 `non_expiring_ads_wan: 50`，不是 500000 个计价单位。
- 年度 Ad 总量仅在用户同时给出月峰值时作为校验。没有月峰值时不要擅自除以 12，应询问月度峰值。
- 每月额度与不过期额度是两个独立池，不能互相抵扣。

### 数量与功能

- 用户、广告账户、AI 规则、定时报表、素材库、YouTube、子渠道应用都填写客户真实需求总量。
- 广告账户脚本按每 100 个向上取整。
- “API”默认指标准 Report API；明确说归因 API 时使用 `attribution_api`；“两类/所有 API”同时设置两者。
- “API不要”写入 `explicit_no: ["report_api"]`；“两类API都不要”同时加入 `report_api` 与 `attribution_api`。
- “试新不要”写入 `explicit_no: ["trial_runs"]`。
- 不要把“不需要”项目写成加购数量 0 来替代 `explicit_no`，因为明确拒绝会影响减去项。
- 用户明确给出“报价、美金、美元、人民币、回款”等金额时，将该金额写入顶层 `actual_quote`。例如“美金11500”表示 `currency: "usd"`、`actual_quote: 11500`，不要把它当作数量或套餐价格。

## 减去项

推荐套餐先按**原活动价总价值**确定，减去项不参与套餐排名。

脚本只对明确输入生成减去项：

- 用户明确给出某项需求，套餐内含量高于需求时，未使用额度按同项加购单价反向扣减。
- 用户明确说 API 或试新“不要/取消/不含”，而推荐套餐包含该权益时，生成相应减去项。
- 未提及的字段不自动扣减。
- **不过期 Ad 创建总额度永远不能作为减去项。**
- VIP 内含流量池没有官方独立扣减价格，只提示人工确认，不自行猜价。
- 用户说“不做减去项”时设置 `deduction_mode: "none"`；否则省略或使用 `"auto"`。禁用后不得生成自动或手动减去项。

## 价格链路

统一使用以下名称：

```text
原活动价总价值 = 套餐官方活动价 + 必要加购
调整后活动价 = 原活动价总价值 − 减去项
输出 6/7/8/9 四档折扣及对应金额，不在对外文案中展开“总价 × 折扣系数”的公式描述。
最终折扣 = 实际报价 ÷ 调整后活动价 × 10
```

未提供实际报价时，用 9 折报价作为“建议实际报价金额”，建议核算折扣为 9.00 折，并输出 6、7、8、9 折四档报价。用户提供具体报价金额时，只输出该报价相对调整后活动价的折扣，不展开 6、7、8、9 折金额。

结构化结果中仍保留 `actual_quote: null`，并把金额写入 `suggested_quote`，避免把系统建议误记为客户已经确认的实际报价。

用户明确给出实际报价时，只展示 `final_discount`，分母固定使用扣减后的调整后活动价。不要再计算或展示相对原活动价总价值的折扣，避免商务同时看到两种折扣口径。

## 输出要求

严格保留 `references/output-template.md` 中的全部区块：

1. 核心结论与最终折扣
2. 推荐说明
3. 计算公式与正负明细
4. 计算推演
5. Ad 创建额度汇总
6. 套餐额度抵扣
7. 四档套餐对比
8. 风险与价格来源

金额旁必须显示币种。报价构成每行必须说明项目、真实数量/计价单位、小计和正负号。

## 可靠性规则

- 使用脚本输出，不凭语言模型补算或四舍五入。
- 保留“未提供”和“明确为 0”的差异。
- 价格快照与官网不一致时停止报价，先更新参考与脚本并标注版本。
- 每次输出必须注明初始化报价单获取时间、获取状态和来源 URL；不要用价格版本日期代替获取时间。
- 当前活动价有效期截至 2026-07-31；到期或价格需要更新时，重新运行初始化脚本刷新快照后再报价。
- 高频 AI 的“套餐价格 × 1.5”按套餐官方活动价总额乘 1.5 处理。
- Postback/S2S 同时需要但计费关系不明确时，提示人工确认，不猜测是否共用数据包。
